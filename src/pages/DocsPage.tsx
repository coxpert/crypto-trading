import { Typography } from "@mui/material"
import { Container } from "@mui/system"

const DocsPage = () => {
    return (
        <Container>
            <Typography variant="h1">Docs Page</Typography>
        </Container>
    )
}

export default DocsPage