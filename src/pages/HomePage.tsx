import { Typography } from "@mui/material"
import { Container } from "@mui/system"

const HomePage = () => {
    return (
        <Container>
            <Typography variant="h1">Dexpools Trading</Typography>
        </Container>
    )
}

export default HomePage
